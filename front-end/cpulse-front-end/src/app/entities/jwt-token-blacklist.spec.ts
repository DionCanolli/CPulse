import { JwtTokenBlacklist } from './jwt-token-blacklist';

describe('JwtTokenBlacklist', () => {
  it('should create an instance', () => {
    expect(new JwtTokenBlacklist()).toBeTruthy();
  });
});
